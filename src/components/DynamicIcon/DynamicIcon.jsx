// Imports
import './styles.css';

// Component start
const DynamicIcon = ({icon}) => {
    return (
        <div className='dynamicIconDiv'>
            {icon}
        </div>
    );
}

// Exports
export default DynamicIcon